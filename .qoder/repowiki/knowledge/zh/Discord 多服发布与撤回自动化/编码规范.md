- 统一通过 `get_cell_val` 函数处理 Aitable 单元格数据的嵌套结构（dict/value/list），确保数据读取的健壮性。
- 所有 Discord API 请求均通过指定的 HTTP 代理（`netproxy.ejoy.com`）进行，以适配网络环境。
- 采用区域化 Token 管理策略，根据目标服务器（JP/TC/SEA/KR）从 `.env` 文件中提取对应的 `DISCORD_{SERVER}_BOT_TOKEN`。